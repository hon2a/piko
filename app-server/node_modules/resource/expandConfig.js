var fn = require('functions');

var expandConfig = module.exports = function (val) {
	if (!fn.isObject(val)) {
		var expanded = {};
		['index', 'create', 'show', 'update', 'destroy'].forEach(function (action) {
			expanded[action] = val;
		});
		return expanded;
	} else {
		return val;
	}
};