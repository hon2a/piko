var email = require('emailjs/email');

var smtp = module.exports = function (options) {

	return function (req, res, next) {
		var server = email.server.connect(options);
		req.smtp = {
			send: function (message, callback) {
				message = fn.merge({
					from: options.from
				}, message);
				server.send(message, callback);
			}
		};
		next();
	};
};