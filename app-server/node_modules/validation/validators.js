var util = require('util'),
	fn = require('functions');

var validators = module.exports = {

	// testing function (not much use otherwise)
	eq: function (val, a) {
		return (val === a) || util.format('Value is not equal to %s.', fn.quote(a));
	}
	
};